[project]
name = "hpe-automation-container"
version = "0.1.0"
description = "Automation container for healthy-person-emulator.org"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.35.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
